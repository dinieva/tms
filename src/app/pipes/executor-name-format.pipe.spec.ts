import { ExecutorNameFormatPipe } from './executor-name-format.pipe';

describe('ExecutorNameFormatPipe', () => {
    it('create an instance', () => {
        const pipe = new ExecutorNameFormatPipe();
        expect(pipe).toBeTruthy();
    });
});
